# Use mock API (true) or real backend (false)
VITE_USE_MOCK=true

# Backend API base URL (used when VITE_USE_MOCK=false)
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Demo accounts (password: secret123)
# aicha@email.com — student
# recruteur@globaltech.cm — verified recruiter
# pending@startup.cm — pending recruiter
# admin@sezame.cm — admin